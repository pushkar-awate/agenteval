# The agenteval core (agentcore + evalcore) is pure Python standard library.
# The dashboard needs Streamlit (and pandas, which ships with it):
streamlit>=1.30

# Later milestones add:
#   sentence-transformers      # embedding judge (CPU)
# Optional: set GROQ_API_KEY to enable the LLM-as-judge (faithfulness/relevance).
